sugar = int(input())
bag = 0
while sugar >= 0:
    if sugar % 5 == 0: #설탕이 5의 배수이면
        bag += (sugar // 5) #5로 나눴을 때 몫
        print(bag)
        break
    sugar -= 3
    bag += 1 #5의 배수로 만들기 위해 설탕-3, 봉지+1
else:
    print(-1)
