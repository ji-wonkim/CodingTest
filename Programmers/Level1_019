---문제---
수많은 마라톤 선수들이 마라톤에 참여하였습니다. 단 한 명의 선수를 제외하고는 모든 선수가 마라톤을 완주하였습니다. 마라톤에 참여한 선수들의 이름이 담긴 배열 participant와 
완주한 선수들의 이름이 담긴 배열 completion이 주어질 때, 완주하지 못한 선수의 이름을 return 하도록 solution 함수를 작성해주세요.


---제한 사항---
마라톤 경기에 참여한 선수의 수는 1명 이상 100,000명 이하입니다.
completion의 길이는 participant의 길이보다 1 작습니다.
참가자의 이름은 1개 이상 20개 이하의 알파벳 소문자로 이루어져 있습니다.
참가자 중에는 동명이인이 있을 수 있습니다.


---예시---
          participant	                     completion         	        return
    ["leo", "kiki", "eden"]           	["eden", "kiki"]	              "leo"
    ["marina", "josipa", "nikola",      ["josipa", "filipa",           "vinko"
     "vinko", "filipa"]	                 "marina", "nikola"]	
["mislav", "stanko", "mislav", "ana"]	 ["stanko", "ana", "mislav"]	   "mislav"
 
 
 ---입출력 예---
 예제 #1
"leo"는 참여자 명단에는 있지만, 완주자 명단에는 없기 때문에 완주하지 못했습니다.

예제 #2
"vinko"는 참여자 명단에는 있지만, 완주자 명단에는 없기 때문에 완주하지 못했습니다.

예제 #3
"mislav"는 참여자 명단에는 두 명이 있지만, 완주자 명단에는 한 명밖에 없기 때문에 한명은 완주하지 못했습니다.


---풀이---
처음 한 생각은 sort() 함수를 이용해 비교하면 된다고 생각했다. 하지만 중복값이 존재하는 마지막 예시만 통과하였다... 😅
검색해보니 zip을 이용한 사람, Counter를 이용한 사람 등등... 여러 풀이가 존재했다. 먼저 zip을 사용한 경우는,

def solution(participant, completion):
    participant.sort()  #정렬
    completion.sort()   
    
    for p, c in zip(participant, completion):
        if p != c:
            return p
    return  participant[-1]

코드를 살펴보면, return participant[-1]을 하지 않을 경우 제일 마지막에 있는 참가자가 완주하지 못할 경우 찾을 수 없다. 따라서 마지막에 participant[-1]을 return 하는 코드를 추가한다.
다음으로 Counter를 사용하는 경우를 살펴보면,

import collections 
def solution(participant, completion): 
	answer = collections.Counter(participant) - collections.Counter(completion)
	return list(answer.keys())[0]

collections.Counter로 빼기를 하여 participant에 있지만 completion에 없는 사람을 찾을 수 있다. return에서 keys를 꺼내와야 하기 때문에 keys를 list로 바꾸고 [0] 값을 출력한다.

#해시
전화번호부에 전화번호를 추가한다고 생각해보자. 가나다 순으로 이름과 번호를 저장하는데 만약 새로 추가되어야 하는 사람의 성이 김씨이면 앞부분에 추가되고 뒤의 번호를 모두 옮겨야 할 것이다. 그렇다고 순서대로 전화번호를 저장하지 않으면 검색에 오래 걸린다. 이렇게 등장하게 된 것이 해싱이다. 
넓은 공간을 사용해 데이터의 저장과 검색 시 시간을 줄이는 것이 해싱의 핵심이라고 할 수 있다.
해시 테이블은 키에 데이터를 저장하는 데이터 구조이다.
